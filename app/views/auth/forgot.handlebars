<div class="d-flex align-items-center justify-content-center min-vh-75">
  <div class="col-12 col-sm-8 col-md-6">
    <div class="card shadow-sm">
      <div class="card-body p-4">
        <h3 class="text-center mb-3">Recuperar contraseña</h3>
        {{#if error}}<div class="alert alert-danger">{{error}}</div>{{/if}}
        {{#if success}}<div class="alert alert-success">{{success}}</div>{{/if}}
        <form action="/auth/forgot" method="POST">
          <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="email" name="email" class="form-control" placeholder="usuario@ejemplo.com" required>
          </div>
          <div class="d-grid">
            <button class="btn btn-primary">Enviar enlace</button>
          </div>
        </form>
        <div class="text-center mt-3"><a href="/auth/jwt/login">Volver al login</a></div>
      </div>
    </div>
  </div>
</div>